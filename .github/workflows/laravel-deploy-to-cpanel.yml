# WORKFLOW REMOVED
# The CI/CD workflow has been removed. Deployments are performed via deploy.php on the server.
# If you want to re-enable GitHub Actions later, restore or create a workflow file here.
